/// Listing kind shown on step 1.
enum AddAdPropertyType {
  singleBed,
  privateRoom,
  entireApartment,
  studio,
}

/// Who can rent (step 1).
enum AddAdGenderPreference {
  male,
  female,
  any,
}
